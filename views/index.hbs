    <div class="wrapper">
      
      <div class="modal fade" id="new-order" tabindex="-1" role="dialog" aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered" role="document">
              <div class="modal-content">
                  <div class="modal-body">
                      <div class="popup text-left">
                          <h4 class="mb-3">New Order</h4>
                          <div class="content create-workform bg-body">
                              <div class="pb-3">
                                  <label class="mb-2">Email</label>
                                  <input type="text" class="form-control" placeholder="Enter Name or Email">
                              </div>
                              <div class="col-lg-12 mt-4">
                                  <div class="d-flex flex-wrap align-items-ceter justify-content-center">
                                      <div class="btn btn-primary mr-4" data-dismiss="modal">Cancel</div>
                                      <div class="btn btn-outline-primary" data-dismiss="modal">Create</div>
                                  </div>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      </div>      
      <div class="content-page">
     <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card card-transparent card-block card-stretch card-height border-none">
                    <div class="card-body p-0 mt-lg-2 mt-0">
                        <h3 class=" text-center">Xin chào {{user.name}},</h3>
                        <h2 class="text-center">Chào mừng đến với Hệ thống Ký Hợp Đồng Số</h2>
                        <h4 class="text-center mt-3">Sử dụng chữ ký số RSA để tạo và xác thực hợp đồng một cách an toàn.</h4>
                        <p class="mb-0 mr-4"></p>
                    </div>
                </div>
            </div>
            </div>          
        </div>
        <div class="text-center mt-5">
        {{#ifEquals user.role 'user'}}
        <p class="mt-3 text-center">Nếu bạn cần cấp lại khóa, vui lòng nhấn vào nút bên dưới:</p>
        {{#ifEquals user.keyResetRequest.requested false}}
        <form action="/users/request-key-reset/{{user.id}}" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-primary mt-2">Yêu cầu cấp lại khóa</button>
        </form>
        {{/ifEquals}}
        {{#ifEquals user.keyResetRequest.requested true}}
        <a href="#" class="btn btn-secondary btn-lg"  disabled>Chờ duyệt để cấp lại khóa</a>
        {{/ifEquals}}
        {{/ifEquals}}   
        </div>
        <!-- Page end  -->
    </div>
    </div>
    </div>


